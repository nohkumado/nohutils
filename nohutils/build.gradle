apply plugin: 'com.android.library'

ext {
    PUBLISH_GROUP_ID = 'com.nohkumado'
    PUBLISH_ARTIFACT_ID = 'nohutils'
    PUBLISH_VERSION = '1.0'
    //bintrayRepo = 'nohutils'
    //bintrayName = 'nohutils'

    //publishedGroupId = 'com.nohkumado'
    //libraryName = 'nohutils'
    //artifact = 'nohutils'

    //libraryDescription = 'A set of utilities that can be used in android apps'

    //siteUrl = 'https://github.com/nohkumado/nohutils'
    //gitUrl = 'https://github.com/nohkumado/nohutils.git'

    //libraryVersion = '1.0.0'

    //developerId = 'nohkumado'
    //developerName = 'Noh Kuma Do'
    //developerEmail = 'nohkumado@gmail.com'

    //licenseName = 'LGPL license, Version 3.0'
    //licenseUrl = 'https://www.gnu.org/licenses/lgpl.txt'
    //allLicenses = ["LGPL-3.0"]
}

android {
    compileSdkVersion 26
    buildToolsVersion '25.0.0'

    defaultConfig {
        //applicationId "com.nohkumado.nohutils"
        minSdkVersion 21
        targetSdkVersion 26
        versionCode 1
        versionName "1.0"
    }
    buildTypes {
        release {
            minifyEnabled false
            proguardFiles getDefaultProguardFile('proguard-android.txt'), 'proguard-rules.pro'
        }
    }
}

dependencies {
    compile fileTree(dir: 'libs', include: ['*.jar'])
}

apply from: 'https://raw.githubusercontent.com/blundell/release-android-library/master/android-release-aar.gradle'

//Add these lines to publish library to bintray. This is the readymade scripts made by github user nuuneoi to make uploading to bintray easy.
//Place it at the end of the file
//apply from: 'https://raw.githubusercontent.com/nuuneoi/JCenter/master/installv1.gradle'
//apply from: 'https://raw.githubusercontent.com/nuuneoi/JCenter/master/bintrayv1.gradle'